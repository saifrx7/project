name: CI Pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node (if using npm tools)
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Run build (example step)
        run: echo "Build successful!"

